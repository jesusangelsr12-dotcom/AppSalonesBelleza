/**
 * Flujo Registrar Cita (5 pasos)
 * Clienta → Servicio → Costo → Método pago → Confirmación
 */

export function render() {
  return `
    <div class="screen">
      <p class="text-center" style="color: var(--color-gray-400)">
        Registrar Cita — pendiente de implementar (Paso 6)
      </p>
    </div>
  `;
}

export function init() {
  // Event listeners se conectarán en el Paso 6
}
