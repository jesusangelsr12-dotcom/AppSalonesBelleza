/**
 * Pantalla Configuración
 * Editar catálogo de servicios
 */

export function render() {
  return `
    <div class="screen">
      <p class="text-center" style="color: var(--color-gray-400)">
        Configuración — pendiente de implementar (Paso 9)
      </p>
    </div>
  `;
}

export function init() {
  // Event listeners se conectarán en el Paso 9
}
