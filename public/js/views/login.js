/**
 * Pantalla de Login
 * Selección de salón + PIN de 4 dígitos
 */

export function render() {
  return `
    <div class="screen screen-centered">
      <p class="text-center" style="color: var(--color-gray-400)">
        Pantalla de Login — pendiente de implementar (Paso 4)
      </p>
    </div>
  `;
}

export function init() {
  // Event listeners se conectarán en el Paso 4
}
