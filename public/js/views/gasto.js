/**
 * Flujo Registrar Gasto (4 pasos)
 * Descripción → Monto → Método pago → Confirmación
 */

export function render() {
  return `
    <div class="screen">
      <p class="text-center" style="color: var(--color-gray-400)">
        Registrar Gasto — pendiente de implementar (Paso 7)
      </p>
    </div>
  `;
}

export function init() {
  // Event listeners se conectarán en el Paso 7
}
