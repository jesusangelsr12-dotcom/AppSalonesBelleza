/**
 * Pantalla Ver Registros de Hoy
 * Citas + Gastos del día con totales
 */

export function render() {
  return `
    <div class="screen">
      <p class="text-center" style="color: var(--color-gray-400)">
        Registros de Hoy — pendiente de implementar (Paso 8)
      </p>
    </div>
  `;
}

export function init() {
  // Event listeners se conectarán en el Paso 8
}
